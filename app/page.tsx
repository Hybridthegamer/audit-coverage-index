// Placeholder only. The real hero (Instrument Serif drift figure) ships in build step 3.
export default function Home() {
  return (
    <main>
      <h1>Audit Coverage Index</h1>
      <p>Build step 1: schema, migrations, drift computation. No UI yet.</p>
    </main>
  );
}
